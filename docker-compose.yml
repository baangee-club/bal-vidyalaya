version: "3"

services:
  server:
    image: ghcr.io/zkhan93/cscbalvid
    entrypoint: ["uvicorn", "main:app", "--reload" ,"--host", "0.0.0.0", "--port", "80"]
    env_file:
      - .env
    ports:
      - 8080:80
